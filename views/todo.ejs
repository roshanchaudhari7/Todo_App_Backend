<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>To Do</title>
  <style>
    body {
      margin: 0;
      padding: 0;
      font-family: sans-serif;
      background-color: #f9f9f9;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }

    .container {
      background-color: #fff;
      padding: 30px 40px;
      border-radius: 12px;
      box-shadow: 0 8px 20px #0000001a;
      width: 400px;
    }

    h1 {
      text-align: center;
      margin-bottom: 25px;
      font-size: 24px;
      color: #333;
    }

    form {
      margin-bottom: 20px;
    }

    .input-group {
      display: flex;
      gap: 10px;
    }

    input[type="text"] {
      flex: 1;
      padding: 12px 14px;
      font-size: 14px;
      border: 1px solid #ccc;
      border-radius: 8px;
      outline: none;
      transition: border-color 0.3s ease;
    }

    input[type="text"]:focus {
      border-color: #e74c3c;
    }

    input::placeholder {
      color: #aaa;
    }

    .btn {
      padding: 12px 16px;
      border: none;
      border-radius: 8px;
      font-size: 14px;
      font-weight: bold;
      cursor: pointer;
      transition: background-color 0.3s ease;
    }

    .btn-primary {
      background-color: #e74c3c;
      color: white;
    }

    .btn-primary:hover {
      background-color: #c0392b;
    }

    ul#item_list {
      list-style: none;
      padding: 0;
      margin: 20px 0;
    }

    ul#item_list li {
      background-color: #f0f0f0;
      padding: 10px 14px;
      margin-bottom: 10px;
      border-radius: 8px;
      font-size: 14px;
      color: #333;
    }

    .btn-full {
      width: 100%;
      background-color: #3498db;
      color: white;
    }

    .btn-full:hover {
      background-color: #2980b9;
    }
  </style>
</head>

<body>
  <div class="container">
    <h1>To-Do App</h1>

    <form id="create_form" action="/create-item" method="POST">
      <div class="input-group">
        <input id="create_field" name="todo" autofocus autocomplete="off" type="text" placeholder="Enter a task..." required />
        <button class="btn btn-primary" type="submit">Add</button>
      </div>
    </form>

    <ul id="item_list">
        
    </ul>

    <form action="/logout" method="POST">
      <button class="btn btn-full" type="submit">Logout</button>
    </form>

    <form action="/logout_from_all_devices" method="POST">
      <button class="btn btn-full" type="submit">Logout from all devices</button>
    </form>
  </div>
</body>

</html>
